import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ShareFlow - High-Quality Screen Sharing',
  description: 'Share your screen with superior quality. No registration required.',
  keywords: 'screen sharing, webrtc, real-time, collaboration, high quality',
  authors: [{ name: 'ShareFlow Team' }],
  viewport: 'width=device-width, initial-scale=1, maximum-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
}